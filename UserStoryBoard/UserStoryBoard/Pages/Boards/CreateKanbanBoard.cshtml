@page
@model UserStoryBoard.Pages.Boards.CreateKanbanBoardModel
@{
    ViewData["Title"] = "CreateKanbanBoard";
}
<h1>Create Kanban Board</h1>

<form method="post">
    <div class="form-group">
        <label asp-for="@Model.KanbanBoard.Name" class="control-label"> Title </label>
        <input asp-for="@Model.KanbanBoard.Name" class="form-control" />
        <span asp-validation-for="@Model.KanbanBoard.Name" class="text-danger"></span>
    </div>
    <div class="form-group">
        <label asp-for="@Model.KanbanBoard.Description" class="control-label"> Description </label>
        <input asp-for="@Model.KanbanBoard.Description" class="form-control" />
        <span asp-validation-for="@Model.KanbanBoard.Description" class="text-danger"></span>
    </div>
    @*<div class="form-group">
        <label asp-for="@Model.KanbanBoard.Columns" class="control-label"> Columns </label>
        <input asp-for="@Model.KanbanBoard.Columns" class="form-control" />
        <span asp-validation-for="@Model.KanbanBoard.Columns" class="text-danger"></span>
    </div>*@

    <div class="form-group">
        <input type="submit" value="Create" class="btn btn-primary" />
    </div>
</form>